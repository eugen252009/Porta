export interface PortaBuildInfo { readonly version: string; readonly commit: string; readonly buildId: string; readonly dirty: boolean | null; readonly builtAt?: string }
export function buildInfo(env: Record<string, string | undefined> = process.env): PortaBuildInfo { const version = env.PORTA_VERSION || "0.1.0"; const commit = env.PORTA_GIT_COMMIT || "unknown"; const buildId = env.PORTA_BUILD_ID || "development"; const dirty = env.PORTA_BUILD_DIRTY === "true" ? true : env.PORTA_BUILD_DIRTY === "false" ? false : null; return { version, commit, buildId, dirty, ...(env.PORTA_BUILT_AT ? { builtAt: env.PORTA_BUILT_AT } : {}) }; }
